server:
  port: 8080

spring:
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://aws-1-eu-central-1.pooler.supabase.com:6543/postgres?prepareThreshold=0&sslmode=require}
    username: ${DB_USERNAME:postgres.gbkngancxgszabhyokrv}
    password: ${DB_PASSWORD:sahleyis201}
    hikari:
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}
      timeout: 5000

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB

app:
  jwt:
    secret: ${JWT_SECRET:fallback-secret-for-development-only}
    expiration-ms: 2592000000  # 30 days

  google:
    client-id: ${GOOGLE_CLIENT_ID:802247549330-j6l3kkqehqedcl7bsgpduro1hihqpc1o.apps.googleusercontent.com}

  supabase:
    url: ${SUPABASE_URL:}
    key: ${SUPABASE_KEY:}

  owner-email: dahiruhammajam@gmail.com

  cors:
    allowed-origins: ${CORS_ORIGINS:http://localhost:3000}
